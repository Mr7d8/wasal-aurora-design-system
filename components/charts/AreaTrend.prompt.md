Trend over time, one series. Smooth curve, soft fill, value called out at the end point instead of a y axis.

```jsx
<AreaTrend values={[12, 18, 16, 27, 24, 38]} labels={["W1","W2","W3","W4","W5","W6"]} valueLabel="38k" accent={0} />
```

For several overlapping series use StackedArea instead.
