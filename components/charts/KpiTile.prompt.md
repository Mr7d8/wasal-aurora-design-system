Three or four of these in a row replace a chart when the data is just headline numbers.

```jsx
<div style={{ display: "flex", gap: "var(--card-gap)" }}>
  <KpiTile value="135s" label="Per product, manual workflow" accent="coral" glyph="clock" />
  <KpiTile value="11s" label="Per product, AI assisted" accent="mint" glyph="bolt" />
</div>
```
