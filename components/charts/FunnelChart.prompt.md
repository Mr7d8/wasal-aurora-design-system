Conversion, or any total splitting down a path. The value rides inside the bar in ink; never white, which fails contrast on every accent. Step names sit outside the bar.

```jsx
<FunnelChart steps={[{ label: "Sessions", value: 9400 }, { label: "Order", value: 840 }]} />
```
