Correlation between two measures. Label each bubble directly above it; drop the legend.

```jsx
<BubbleChart points={[{ label: "Beauté", x: 42, y: 58, r: 26 }]} xLabel="Spend" yLabel="Revenue" />
```
