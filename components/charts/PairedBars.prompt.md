Same category, two measures. The comparison period always takes the lighter tint of the same accent.

```jsx
<PairedBars groups={[{ label: "Mode", a: 78, b: 44 }]} seriesNames={["This month", "Last month"]} />
```
