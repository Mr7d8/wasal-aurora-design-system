Leads a glass card or a list row. One accent per theme, held consistent across a deck.

```jsx
<IconChip glyph="search" accent="mint" />
```

Glyphs are Lucide outlines at 1.5px stroke. Add new ones to `glyphs.js` rather than inlining an SVG at the call site.
