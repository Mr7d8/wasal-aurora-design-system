One gradient phrase per slide at most.

```jsx
<GradientHeadline size="var(--size-display)">Proof, not theory</GradientHeadline>
```

On feature slides keep the headline in ink and gradient only the last phrase with an inline span, or use a flat accent instead.
