Centerpiece of a hub slide, or an unlabeled backdrop behind a phone frame.

```jsx
<GradientOrb size={340}>One assistant,<br />four places</GradientOrb>
```

Leave it unlabeled and larger (520 to 640px, no children) when it sits behind a device.
